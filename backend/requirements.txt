# --- Core web stack (Django + DRF + Auth) ---
Django==5.2
djangorestframework==3.16.0
djangorestframework-simplejwt==5.5.1
PyJWT==2.9.0
django-cors-headers==4.7.0
django-filter==25.1
django-environ==0.12.0
django-phonenumber-field==8.1.0
phonenumbers==9.0.7
django-two-factor-auth==1.17.0
django-otp==1.6.0
django-import-export==4.3.9
tablib==3.8.0

# --- ASGI / realtime ---
channels==4.2.2
channels-redis==4.2.1

# --- Task queue ---
celery==5.5.3
kombu==5.5.4
billiard==4.2.1

# --- Cache / broker / client libs ---
redis==5.0.8        # use a 5.x redis-py client; avoid 6.2.0 here
aioredis==2.0.1

# --- Database drivers (pick one engine per DB you actually use) ---
# psycopg[binary]==3.2.9   # PostgreSQL (modern psycopg3)
psycopg2-binary==2.9.9 # (ALTERNATIVE: if you prefer psycopg2)
PyMySQL==1.1.1           # MySQL (remove if not needed)
# aiomysql==0.2.0        # Async MySQL (only if you actually use it)

# --- Supabase / VGS / related ---
supabase==2.16.0
gotrue==2.11.4
postgrest==1.1.1
realtime==2.5.3
storage3==0.12.1
vgs-api-client==0.0.51

# --- API frameworks (keep only what you ship) ---
# FastAPI stack (comment out if not running FastAPI in prod)
fastapi==0.111.1
starlette==0.37.2
uvicorn==0.30.1

# --- Security / crypto ---
cryptography==42.0.8
pyOpenSSL==24.1.0
python-dotenv==1.0.1

# --- Utilities ---
requests==2.32.3
httpx==0.27.2
pydantic==2.11.7
typing_extensions==4.14.1
gunicorn==23.0.0
whitenoise==6.10.0
